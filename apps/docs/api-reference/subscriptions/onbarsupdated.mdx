---
title: "onBarsUpdated"
description: "Live-streamed bar chart data to track price changes over time."
---

**Returns:** [OnBarsUpdatedResponse](/api-reference/types/onbarsupdatedresponse)

### Arguments

<ResponseField name="pairId" type="String">
  The ID of the pair (`address`:`networkId`).
</ResponseField>

<a href="/api-reference/types/quotetoken">
  <ResponseField name="quoteToken" type="QuoteToken">
    The token of interest within the pair. Can be `token0` or `token1`.
  </ResponseField>
</a>

### Schema


```graphql 
"""Live-streamed bar chart data to track price changes over time."""
onBarsUpdated(pairId: String, quoteToken: QuoteToken): OnBarsUpdatedResponse
```
