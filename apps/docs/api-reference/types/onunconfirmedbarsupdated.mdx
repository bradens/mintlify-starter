---
title: "OnUnconfirmedBarsUpdated"
description: "Response returned by `onUnconfirmedBarsUpdated`."
---

### Fields

<ResponseField name="pairAddress" type="String!" required>
  The contract address for the pair.
</ResponseField>

<ResponseField name="networkId" type="Int!" required>
  The network ID the pair is deployed on.
</ResponseField>

<ResponseField name="pairId" type="String!" required>
  The ID for the pair (`pairAddress`:`networkId`).
</ResponseField>

<ResponseField name="timestamp" type="Int!" required>
  The unix timestamp for the new bar.
</ResponseField>

<ResponseField name="eventSortKey" type="String!" required>
  The sortKey for the bar (`blockNumber`#`transactionIndex`#`logIndex`, zero padded). For example, `0000000016414564#00000224#00000413`.
</ResponseField>

<a href="/api-reference/types/unconfirmedresolutionbardata">
  <ResponseField name="aggregates" type="UnconfirmedResolutionBarData!" required>
    Price data broken down by resolution.
  </ResponseField>
</a>

<a href="/api-reference/types/quotetoken">
  <ResponseField name="quoteToken" type="QuoteToken">
    The quote token within the pair.
  </ResponseField>
</a>

<ResponseField name="quoteTokenAddress" type="String!" required>
  The address of the token being quoted
</ResponseField>


```graphql OnUnconfirmedBarsUpdated
"""Response returned by `onUnconfirmedBarsUpdated`."""
type OnUnconfirmedBarsUpdated {
  """The contract address for the pair."""
  pairAddress: String!
  """The network ID the pair is deployed on."""
  networkId: Int!
  """The ID for the pair (`pairAddress`:`networkId`)."""
  pairId: String!
  """The unix timestamp for the new bar."""
  timestamp: Int!
  """The sortKey for the bar (`blockNumber`#`transactionIndex`#`logIndex`, zero padded). For example, `0000000016414564#00000224#00000413`."""
  eventSortKey: String!
  """Price data broken down by resolution."""
  aggregates: UnconfirmedResolutionBarData!
  """The quote token within the pair."""
  quoteToken: QuoteToken
  """The address of the token being quoted"""
  quoteTokenAddress: String!
}
```
