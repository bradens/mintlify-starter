---
title: "holders"
description: "Returns list of wallets that hold a given token, ordered by holdings descending. Also has the unique count of holders for that token."
---

**Returns:** [HoldersResponse!](/api-reference/types#holdersresponse)

### Arguments

<a href="/api-reference/input-objects#holdersinput">
  <ResponseField name="input" type="HoldersInput!" required>
    No description provided
  </ResponseField>
</a>

### Schema


```graphql 
"""Returns list of wallets that hold a given token, ordered by holdings descending. Also has the unique count of holders for that token."""
holders(input: HoldersInput!): HoldersResponse!
```
