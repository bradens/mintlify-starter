---
title: "getNetworkStats"
description: "Returns metadata for a given network supported on Codex."
---

**Returns:** [GetNetworkStatsResponse](/api-reference/types/getnetworkstatsresponse)

### Arguments

<ResponseField name="networkId" type="Int!" required>
  The network Id to filter by.
</ResponseField>

<ResponseField name="exchangeAddress" type="String">
  The exchange address to filter by (optional).
</ResponseField>

### Schema


```graphql 
"""Returns metadata for a given network supported on Codex."""
getNetworkStats(networkId: Int!, exchangeAddress: String): GetNetworkStatsResponse
```
