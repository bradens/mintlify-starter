---
title: "DetailedNftStatsNumberMetrics"
description: "Number metrics for detailed NFT stats."
keywords: ['DetailedNftStatsNumberMetrics']
---

### Fields

<ResponseField name="change" type="Float">
  The percent change between the `currentValue` and `previousValue`.
</ResponseField>

<ResponseField name="currentValue" type="Int">
  The total value for the most recent duration.
</ResponseField>

<ResponseField name="previousValue" type="Int">
  The total value for the previous duration.
</ResponseField>

<ResponseField name="buckets" type="[Int]!" required>
  The list of aggregated values for each bucket.
</ResponseField>

### Schema


```graphql 
"""Number metrics for detailed NFT stats."""
type DetailedNftStatsNumberMetrics {
  """The percent change between the `currentValue` and `previousValue`."""
  change: Float
  """The total value for the most recent duration."""
  currentValue: Int
  """The total value for the previous duration."""
  previousValue: Int
  """The list of aggregated values for each bucket."""
  buckets: [Int]!
}
```
