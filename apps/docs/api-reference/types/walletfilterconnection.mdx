---
title: "WalletFilterConnection"
description: "A connection of wallets matching a filter."
keywords: ['WalletFilterConnection']
---

### Fields

<a href="/api-reference/types/walletfilterresult">
  <ResponseField name="results" type="[WalletFilterResult!]!" required>
    The list of wallets matching the filter parameters.
  </ResponseField>
</a>

<ResponseField name="count" type="Int!" required>
  The number of wallets returned.
</ResponseField>

<ResponseField name="offset" type="Int!" required>
  Where in the list the server started when returning items.
</ResponseField>

### Schema


```graphql 
"""A connection of wallets matching a filter."""
type WalletFilterConnection {
  """The list of wallets matching the filter parameters."""
  results: [WalletFilterResult!]!
  """The number of wallets returned."""
  count: Int!
  """Where in the list the server started when returning items."""
  offset: Int!
}
```
