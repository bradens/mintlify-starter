---
title: "onPriceUpdated"
description: "Live-streamed price updates for a token."
keywords: ['onPriceUpdated']
---

**Returns:** [Price](/api-reference/types/price)

### Arguments

<ResponseField name="address" type="String">
  The token contract address.
</ResponseField>

<ResponseField name="networkId" type="Int">
  The network ID the token is deployed on.
</ResponseField>

<ResponseField name="sourcePairAddress" type="String">
  The pair contract address from which to get pricing
</ResponseField>

### Schema


```graphql 
"""Live-streamed price updates for a token."""
onPriceUpdated(address: String, networkId: Int, sourcePairAddress: String): Price
```
