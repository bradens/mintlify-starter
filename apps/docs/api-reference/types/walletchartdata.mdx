---
title: "WalletChartData"
description: "The data for a chart of a wallet's activity."
noindex: true
---

### Fields

<ResponseField name="timestamp" type="Int!" required>
  The timestamp
</ResponseField>

<ResponseField name="resolution" type="String!" required>
  The resolution
</ResponseField>

<ResponseField name="volumeUsd" type="String!" required>
  The volume in USD
</ResponseField>

<ResponseField name="volumeUsdAll" type="String!" required>
  The volume in USD including tokens sold for which we do not have a price
</ResponseField>

<ResponseField name="realizedProfitUsd" type="String!" required>
  The realized profit in USD
</ResponseField>

<ResponseField name="swaps" type="Int!" required>
  The number of swaps
</ResponseField>

### Schema


```graphql 
"""The data for a chart of a wallet's activity."""
type WalletChartData {
  """The timestamp"""
  timestamp: Int!
  """The resolution"""
  resolution: String!
  """The volume in USD"""
  volumeUsd: String!
  """The volume in USD including tokens sold for which we do not have a price"""
  volumeUsdAll: String!
  """The realized profit in USD"""
  realizedProfitUsd: String!
  """The number of swaps"""
  swaps: Int!
}
```
